module.exports = {
  // configureWebpack: config => {
  //   config.externals = {
  //     ...config.externals,
  //       ...process.env.NODE_ENV === 'test' ? null:{'moment': 'moment'}
  //   }
  // }
}
